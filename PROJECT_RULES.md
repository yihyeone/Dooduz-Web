# 두더지도굴단 도감 운영 규칙

이 문서는 두더지도굴단 도감 앱을 수정할 때 반드시 먼저 확인하는 작업 기준이다.
새 채팅/새 작업에서도 이 규칙을 우선 적용한다.

## 0. 최우선 원칙

- 도감 앱 작업에서는 **이미지 생성 도구를 사용하지 않는다.**
- 사용자가 첨부한 게임 캡처 또는 연결된 Google Drive의 원본 게임 캡처에서 **실제 카드 이미지를 크롭하여 사용**한다.
- 꽃 이름, 등급, 보유자 등 운영 정보는 **현재 Google Sheet를 기준 데이터(source of truth)** 로 본다.
- 사용자가 이름/등급/매핑 오류를 직접 정정한 경우 **사용자 정정을 최우선으로 반영**한다.
- 확실하지 않은 꽃 이름이나 카드가 있으면 추정 등록하지 말고 사용자에게 확인을 요청한다.

---

# 1. 캡처 이미지가 첨부됐을 때 기본 처리 순서

사용자가 게임 도감 캡처를 첨부하고 다음과 같이 요청하면:

- "신규 꽃 업데이트해줘"
- "신규/누락 꽃 확인해줘"
- "이미지 없는 꽃 업데이트해줘"
- "도감앱 업데이트해줘"

반드시 아래 순서로 처리한다.

1. 캡처에서 보이는 꽃 이름과 등급을 식별한다.
2. Google Sheet의 기존 꽃 목록과 대조한다.
3. 각 카드를 아래 3종으로 분류한다.
   - **신규 꽃**: 시트에 꽃 이름 자체가 없음
   - **기존 꽃 + 이미지 누락**: 시트에는 있으나 앱 이미지 매핑이 없거나 깨짐
   - **기존 꽃 + 이미지 정상**: 시트와 이미지 모두 정상
4. 신규/누락만 수정 대상으로 삼는다.
5. 기존 정상 꽃을 중복 등록하지 않는다.
6. 수정 후 이름 중복, 이미지 경로 중복, 깨진 경로를 재검사한다.

---

# 2. 신규 꽃 등록 규칙

## 2-1. 신규 여부 판정

신규 꽃은 반드시 **Google Sheet에 동일 꽃 이름이 없는 경우에만** 신규로 본다.

이름 비교 시 다음을 함께 확인한다.

- 공백 차이
- `·`, `ㆍ`, `•` 등의 구분점 차이
- 띄어쓰기 차이
- 과거 오타/별칭

비슷한 이름이 이미 있으면 새 꽃으로 추가하지 말고 기존 항목인지 먼저 확인한다.

## 2-2. 신규 꽃 등록은 정보 + 이미지가 한 세트

신규 꽃을 등록할 때는 반드시 다음 두 작업을 함께 완료한다.

### A. 꽃 정보 시트 업데이트

Google Sheet에 최소 다음 정보를 추가한다.

- 꽃 이름
- 등급 (UR / SSR / SR / R / N)
- 보유자 닉네임: 캡처/사용자 정보로 확인 가능한 경우만 입력

확인되지 않은 값을 임의로 만들어 넣지 않는다.

### B. 꽃 이미지 등록

- 첨부 또는 Drive 원본 캡처에서 해당 카드 전체를 크롭한다.
- WebP로 저장한다.
- `/images/flower-assets/` 아래에 독립 파일로 저장한다.
- base64/data URI로 HTML/JS 안에 넣지 않는다.
- 운영 이미지 매핑에 해당 꽃 이름 → WebP 경로를 연결한다.

신규 꽃은 **시트 정보만 추가하고 이미지 등록을 빼먹거나, 이미지 파일만 추가하고 시트 등록을 빼먹으면 완료로 보지 않는다.**

---

# 3. 기존 꽃의 이미지 누락/엑박 복구 규칙

다음 조건이면 신규 꽃으로 추가하지 않는다.

- Google Sheet에 꽃 이름이 이미 존재함
- 앱에서 이미지가 비어 있음
- 엑박이 뜸
- 잘못된 이미지가 연결됨

이 경우에는 **기존 꽃의 이미지 매핑만 복구/교체**한다.

처리 순서:

1. Google Sheet에서 정확한 꽃 이름 확인
2. 현재 `app.html`, `index.html`, `guild-catalog-images.js`의 매핑 확인
3. 해당 경로의 WebP 파일이 실제로 존재하는지 확인
4. 파일이 없거나 크롭이 잘못됐으면 사용자 캡처/Drive 원본에서 다시 카드 크롭
5. 새 WebP 저장
6. 아래 3개 운영 매핑을 동일하게 갱신
   - `app.html`
   - `index.html`
   - `guild-catalog-images.js`
7. 동일 꽃 이름으로 중복 키가 생기지 않았는지 확인
8. 배포 후 실제 운영 경로가 새 파일을 참조하는지 확인

**시트에 이미 있는 꽃은 절대 새 행으로 중복 추가하지 않는다.**

## 3-1. 관리자 페이지로 등록한 이미지

- `admin.html`에서 등록하거나 교체한 이미지는 `admin-flower-images.json`에 기록한다.
- `admin-flower-images.json`은 `app.html`, `index.html`, `guild-catalog-images.js`의 과거 매핑보다 마지막에 적용되는 운영 오버라이드다.
- 따라서 관리자 페이지를 통한 작업은 세 파일을 개별 수정한 것과 동일하게 전체 운영 화면에 적용된다.
- 수동 이미지 작업에는 기존 3개 매핑 점검 규칙을 그대로 적용한다.

---

# 4. 카드 이미지 크롭 규칙

모든 카드 이미지는 기존 정상 카드와 시각적으로 동일한 크기감이어야 한다.

## 기준

- 카드 전체 테두리가 보이도록 크롭
- 꽃 오브제만 타이트하게 확대하지 않음
- 상단 등급/꽃 아이콘 영역 포함
- 하단 꽃 이름 영역 포함
- 좌우 카드 프레임 여백 유지
- 다른 정상 카드와 크기감/여백이 비슷해야 함
- 기본 비율: **212:279**
- 현재 표준 저장 크기: **424 × 558 px**
- 포맷: WebP

## 금지

- 꽃 부분만 잘라 확대
- 카드 프레임 일부 잘림
- 캡처마다 임의의 크롭 비율 적용
- 이미지 생성 AI로 카드 재생성
- `object-fit`에 의존해 잘못된 원본 크롭을 숨기기

크롭 결과가 애매하면 기존 정상 카드 1장을 기준 샘플로 비교한 뒤 저장한다.

---

# 5. 이름/오타 처리 규칙

- Google Sheet의 현재 이름을 기본 정식명으로 사용한다.
- 사용자가 "이 이름이 맞다"고 직접 정정하면 사용자 지시를 우선한다.
- 과거 오타가 이미지 매핑 키로 남아 있는 경우:
  - 정식명 키를 반드시 추가/수정
  - 필요하면 과거 오타 키는 호환용 alias로만 유지
  - 신규 데이터 행을 만들지 않는다.

예: `명월유합`처럼 사용자가 정식명을 직접 확인한 경우 해당 이름을 기준으로 고정한다.

---

# 6. 중복 검사 규칙

매 업데이트 후 반드시 다음을 확인한다.

## 데이터 중복

- 동일 꽃 이름이 시트에 2번 이상 등록됐는지
- 공백/구분점만 다른 사실상 동일 이름이 있는지

## 이미지 중복

- 동일 꽃이 서로 다른 이미지 경로에 불필요하게 중복 연결됐는지
- 서로 다른 꽃이 실수로 같은 잘못된 카드 이미지를 공유하는지

## 매핑 중복

- `app.html`
- `index.html`
- `guild-catalog-images.js`

세 곳의 꽃 이름 → 이미지 경로가 서로 불일치하지 않는지 확인한다.

---

# 7. 사용자가 캡처 여러 장을 올렸을 때

- 캡처 전체를 먼저 대조하고 한 장씩 즉시 등록하지 않는다.
- 같은 꽃이 여러 캡처에 반복되면 가장 선명하고 카드 테두리가 완전한 원본을 사용한다.
- 활성/비활성 상태 차이만 있고 동일 꽃이면 새 꽃으로 추가하지 않는다.
- 이미지가 이미 정상 등록된 꽃은 다시 저장하지 않는다.

---

# 8. Google Drive 활용 규칙

사용자가 Drive 폴더/파일을 연결해둔 경우:

- 필요한 꽃 카드가 현재 채팅 첨부에 없으면 Drive 원본을 먼저 검색한다.
- Drive에 동일 꽃의 더 선명한 전체 카드 캡처가 있으면 그것을 우선 사용한다.
- Drive에도 원본이 없을 때만 사용자에게 해당 꽃의 게임 캡처를 요청한다.

즉, 이미지가 없다고 바로 사용자에게 재요청하지 말고 **Drive → 현재 첨부 → 과거 저장 원본** 순서로 확인한다.

---

# 9. 완료 조건

아래가 모두 끝나야 "업데이트 완료"라고 보고한다.

- 신규 꽃이면 Google Sheet 정보 등록 완료
- 필요한 WebP 이미지 저장 완료
- 운영 매핑 반영 완료
- `app.html` 반영 확인
- `index.html` 반영 확인
- `guild-catalog-images.js` 반영 확인
- 중복/깨진 경로 검사 완료
- JavaScript 문법 검사 통과
- GitHub Actions 성공 확인(사용 시)
- Vercel 배포 성공 확인

중간 단계만 끝났는데 완료라고 말하지 않는다.

---

# 10. 작업 요청별 해석 규칙

### "신규 꽃 업데이트해줘"
→ 캡처와 시트를 대조하여 **시트에 없는 꽃만** 정보 + 이미지 함께 등록

### "이미지 없는 꽃 업데이트해줘"
→ 시트는 건드리지 않고 **기존 꽃의 누락 이미지만** 복구

### "신규/누락 꽃 업데이트해줘"
→ 신규는 시트 + 이미지, 누락은 이미지 매핑만 수정

### "이 꽃 이미지 교체해줘"
→ 신규 생성 금지. 기존 캡처/Drive에서 정확한 카드 크롭 후 이미지 경로 교체

### "중복 확인해줘"
→ 꽃 이름 중복 + 사실상 동일 이름 + 이미지 중복 + 매핑 불일치까지 함께 검사

---

# 11. 도굴앱 작업에서 절대 하지 않을 것

- 이미지 생성 도구 호출
- 이름이 불확실한 꽃을 추정 등록
- 시트에 이미 있는 꽃을 신규로 중복 추가
- 꽃 정보만 추가하고 이미지 누락
- 이미지 파일만 추가하고 운영 매핑 누락
- 카드 프레임이 잘린 타이트 크롭
- base64 이미지 재도입
- 배포/검증 전 "완료" 보고

---


## 12. 화면 기본 기능 보존 규칙

아래 기능은 도감 앱의 **고정 기본 상태**다. 꽃 데이터, 이미지, 카드, 팝업, 정렬 등 다른 기능을 수정하더라도 제거하거나 동작을 깨뜨리지 않는다.

- 홈 화면 상단 제목 탭 고정
- 홈 화면 검색창·필터 영역 고정
- 상단 고정 탭과 검색·필터 영역 사이의 틈을 아이보리색으로 막아 스크롤 콘텐츠가 비치지 않게 유지
- 화면 하단 안내/메뉴 영역 고정
- 일정 거리 이상 스크롤하면 TOP 버튼 노출
- TOP 버튼 클릭 시 부드럽게 페이지 최상단으로 이동
- 고정 영역이 도감 완성도 그래프나 다른 콘텐츠를 덮지 않도록 유지

모든 화면 수정 후 위 항목을 회귀 검사한다.

## 작업 시작 체크리스트

도굴앱 수정 요청을 받으면 매번 아래 순서로 시작한다.

1. `PROJECT_RULES.md` 확인
2. 현재 Google Sheet에서 꽃 이름/등급 확인
3. 현재 GitHub 운영 매핑 확인
4. 첨부/Drive 캡처와 대조
5. 신규 / 이미지 누락 / 정상으로 분류
6. 필요한 항목만 수정
7. 중복 검사
8. 배포 검증
9. 완료 보고
